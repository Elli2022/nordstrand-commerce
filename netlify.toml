[build]
  command = "npm ci && npm run build --workspace @nordstrand/shared && npm run build --workspace @nordstrand/web"
  publish = "apps/web"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
