[build]
  base = "apps/web"
  command = "cd ../.. && npm ci && npm run build --workspace @nordstrand/shared && npm run build --workspace @nordstrand/web"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
